{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main Stellar Launcher window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
